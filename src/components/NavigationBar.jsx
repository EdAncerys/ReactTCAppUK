import React from 'react';

export default function NavigationBar({ props }) {
  return (
    <div style={styles.container}>
      <div>NavBar</div>
    </div>
  );
}

const styles = {
  container: {},
};
